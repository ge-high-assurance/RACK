#!/usr/bin/env swipl

:- initialization(main, main).

:- ensure_loaded('./paths').

:- use_module(library(optparse)).
:- consult(common_opts).
:- consult(rack(check)).

main :-
    parse_args([], _Opts, _PosArgs),
    check_rack.
